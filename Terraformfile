pipeline {
    agent any

    stages {
        stage('Initialize')
        {
            steps{
                script{ 
                    sh "pwd"
                    sh "ls -ltr"
                    sh "terraform init"
                    sh "cat /root/.aws/credentials"
                    sh "terraform plan"
                }
            }
        }
    }
}

pipeline {
    agent any

    stages {
        stage('Initialize')
        {
            steps{
                script{ 
                    sh "pwd"
                    sh "ls -ltr"
                    sh "terraform init"
                    sh "terraform plan"
                }
            }
        }
    }
}
